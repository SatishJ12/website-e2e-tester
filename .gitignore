# Encrypted per-site credentials — DPAPI-encrypted but still never commit these
/Creds/*
!/Creds/.gitkeep

# Generated test run output (screenshots, Word/Excel reports) — regenerated each run
/TestRun_*/

# OS junk
.DS_Store
Thumbs.db
*.log
